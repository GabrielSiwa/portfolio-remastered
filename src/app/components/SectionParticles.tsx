export default function SectionParticles({ count }: { count?: number }): null {
  // Disabled per user request — no-op
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  const _unused = count;
  return null;
}
